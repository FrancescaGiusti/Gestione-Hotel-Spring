spring:

  datasource:
    url: jdbc:mysql://localhost:3306/gestione-hotel-test
    username: root
    password: 12345

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
